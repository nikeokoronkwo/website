import { definePage } from "#client";

export default definePage({
    handler: (req) => (
        <div>
            {/* Header */}
            <div></div>
            <div>
                
            </div>
        </div>
    )
})